package ferdi.networkbuilder.model.contacts;
                // System.out.println(set.size() + ":|"+agent.getId()+ "| + |" + v1.getId()+"|" );

public class FriendshipRelation implements Relation{
}
